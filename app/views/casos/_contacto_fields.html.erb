<%= f.simple_fields_for :persona do |p| %>
	<%= render 'persona_fields', :f => p %>
<% end %>
<%= f.association :profesion,
	collection: Profesion.all.order(:nombre),
	include_blank: false,
	label: "Profesión",
	label_method: :nombre, 
	value_method: :id 
%>
<%= f.association :rangoedad,
	include_blank: false,
	label: "Rango de Edad",
	label_method: :rango, 
	value_method: :id 
%>
<%= f.association :filiacion,
	collection: Filiacion.all.order(:nombre),
	include_blank: false,
	label: "Filiación",
	label_method: :nombre, 
	value_method: :id 
%>
<%= f.association :organizacion,
	collection: Organizacion.all.order(:nombre),
	include_blank: false,
	label: "Organización",
	label_method: :nombre, 
	value_method: :id 
%>
<%= f.association :vinculoestado,
	collection: Vinculoestado.all.order(:nombre),
	include_blank: false,
	label: "Vínculo con el Estado",
	label_method: :nombre, 
	value_method: :id
  #wrapper_html: { style: "padding-bottom: 18px;" }
%>
<%= f.association :etnia,
	collection: Etnia.all.order(:nombre),
	include_blank: false,
	label: "Etnia",
	label_method: :nombre, 
	value_method: :id 
%>
<%= f.association :iglesia,
	collection: Iglesia.all.order(:nombre),
	include_blank: false,
	label: "Religión/Iglesia",
	label_method: :nombre, 
	value_method: :id 
%>
<%= f.input(:orientacionsexual, collection: [
						[:HETEROSEXUAL, :H], [:LESBIANA, :L], [:GAY, :G], 
						[:BISEXUAL, :B], [:TRANSEXUAL, :T] 
], 
	label: "Orientación Sexual",
	include_blank: false ) 
%>
<%= f.input :id, as: :hidden %>

